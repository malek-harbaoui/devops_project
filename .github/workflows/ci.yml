name: Python CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pytest and generate reports
        run: |
          mkdir -p reports/allure
          mkdir -p reports/coverage-html
          python -m pytest tests/ \
            --junitxml=reports/junit-report.xml \
            --html=reports/pytest-report.html \
            --self-contained-html \
            --cov=. \
            --cov-report=xml:reports/coverage.xml \
            --cov-report=html:reports/coverage-html \
            --alluredir=reports/allure

      - name: Install Allure CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip
          wget https://github.com/allure-framework/allure2/releases/download/2.21.0/allure-2.21.0.zip
          unzip allure-2.21.0.zip -d /opt/
          echo "PATH=$PATH:/opt/allure-2.21.0/bin" >> $GITHUB_ENV

      - name: Generate Allure HTML dashboard
        run: |
          mkdir -p reports/allure-html
          /opt/allure-2.21.0/bin/allure generate reports/allure -o reports/allure-html --clean

      - name: Create CI dashboard index.html
        run: |
          cat <<EOT > reports/index.html
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <title>CI Reports Dashboard</title>
            <style>
              body { font-family: Arial, sans-serif; margin: 2rem; }
              h1 { color: #2c3e50; }
              ul { list-style-type: none; padding: 0; }
              li { margin: 0.5rem 0; }
              a { text-decoration: none; color: #3498db; }
              a:hover { text-decoration: underline; }
            </style>
          </head>
          <body>
            <h1>CI Reports Dashboard</h1>
            <ul>
              <li><a href="pytest-report.html">Pytest HTML Report</a></li>
              <li><a href="coverage-html/index.html">Coverage Report</a></li>
              <li><a href="allure-html/index.html">Allure Dashboard</a></li>
            </ul>
          </body>
          </html>
          EOT

      - name: Zip Allure HTML report for attachment
        run: |
          cd reports
          zip -r allure-html.zip allure-html
          cd ..

      - name: Upload test reports as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: reports/

  deploy:
    name: Deploy
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download test reports
        uses: actions/download-artifact@v4
        with:
          name: test-reports
          path: reports

      - name: Publish reports to GitHub Pages
        id: deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./reports
          user_name: github-actions
          user_email: github-actions@github.com
          keep_files: true

      - name: Send email with reports
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          from: ${{ secrets.SMTP_USERNAME }}    # Must provide a "from" address
          subject: ${{ github.workflow }} CI Pipeline Report - Commit ${{ github.sha }} - ${{ job.status }}
          to: ${{ secrets.EMAIL_RECEIVER }}
          cc: ${{ secrets.EMAIL_CC }}           # Professor email
          body: |
            Hello,

            The latest CI/CD pipeline run has completed for commit: ${{ github.sha }}
            Status: ${{ job.status }}

            ✅ Test Results Links:
            - Pytest HTML Report: https://malek-harbaoui.github.io/devops_project/pytest-report.html
            - Coverage Report: https://malek-harbaoui.github.io/devops_project/coverage-html/index.html
            - Allure Dashboard: https://malek-harbaoui.github.io/devops_project/allure-html/index.html

            Please find attached the test reports for local inspection (JUnit XML, Pytest HTML, Coverage XML, and Allure ZIP).

            Best regards,
            GitHub Actions
          attachments: |
            reports/junit-report.xml
            reports/pytest-report.html
            reports/coverage.xml
            reports/allure-html.zip





 










            


